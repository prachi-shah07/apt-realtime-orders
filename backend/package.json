{
  "name": "apt-realtime-orders-backend",
  "version": "1.0.0",
  "description": "Real-time order update system — PostgreSQL LISTEN/NOTIFY + WebSockets. APT internship assignment.",
  "main": "src/index.js",
  "scripts": {
    "prestart": "npx -y kill-port 3000 && node create_db.js",
    "start": "node src/index.js",
    "dev": "nodemon src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
